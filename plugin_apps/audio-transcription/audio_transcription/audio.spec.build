# -*- mode: python ; coding: utf-8 -*-
from PyInstaller.utils.hooks import collect_submodules

# get virtualenv path
runtime_venvdir=os.environ['VIRTUAL_ENV'] + "\\Lib\\site-packages"

# to get dependent binaries : whisper\assets\mel_filters.npz

whisper_path = runtime_venvdir + "\\whisper"

# copy ffmpeg.exe from download to this folder from  https://www.ffmpeg.org/download.html as per plugin pre-req doc.

hiddenimports = ['flask_ml']
hiddenimports = ['whisper']
hiddenimports += collect_submodules('ml.model')


a = Analysis(
    ['server.py'],
    pathex=['.'],
    binaries=[('ffmpeg.exe', "."),(whisper_path, 'whisper')],
    datas=[('.\\app-info.md', '.')],
    hiddenimports=hiddenimports,
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='audio',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)

coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='audio')